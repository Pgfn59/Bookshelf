<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Bookshelf" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:textColor">@color/grayBlack</item>
        <item name="android:textColorPrimary">@color/grayBlack</item>
        <item name="android:textColorSecondary">@color/grayBlack</item>
        <item name="android:textColorHint">#8F8F8F</item>
        <item name="android:listDivider">@color/lightGray</item>
    </style>

    <style name="Theme.NavigationToolBar" parent="Theme.MaterialComponents.DayNight.NoActionBar" />
</resources>